export default {
  config: {
    // Prose width varies by the source and is not meaningful for rendered copy.
    MD013: false,
    // Some project documentation uses command snippets without a language.
    MD040: false,
  },
}
