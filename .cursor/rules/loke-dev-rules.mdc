---
description: 
globs: 
alwaysApply: true
---
- Always use Tailwind CSS version 4 for styling and utilize shadcn ui to add components that they support and we dont currently dont have in the codebase, utilize radix ui for components structure.
- Try to avoid useEffect whenever possible.
- Always await params in nextjs, like this const { hello } = await params
- Always add 'use client' at top of file, for components and pages when they have client side logic, like useState.
- Never leave any comments in the code
- Don't write any comments in the code
- Never create test pages or test blog posts
